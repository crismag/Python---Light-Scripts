# parsers/

Scripts for low-level PDF structure inspection and parsing.

Planned scripts:
- `pdf_info.py` — dump the full PDF info dictionary, page sizes, and embedded object counts
- `pdf_fonts.py` — list all fonts embedded in a PDF
