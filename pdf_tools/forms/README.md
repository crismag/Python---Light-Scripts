# forms/

Scripts for creating interactive PDF forms with AcroForm fields.

Planned scripts:
- `create_form.py` — create a PDF with text input, checkbox, and dropdown AcroForm fields
- `form_schema.py` — define and validate form field schemas as Python dataclasses
