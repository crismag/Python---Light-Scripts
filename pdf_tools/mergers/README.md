# mergers/

Scripts that combine or split existing PDF files using pypdf.

Scripts in this folder:
- `pdf_merge.py` — INTERMEDIATE: merge multiple PDF files into one output; never overwrites inputs
- `pdf_split.py` — INTERMEDIATE: split a PDF into per-page files or named page-range files in an output directory
